<Window x:Class="MediaPlayer.Visual.AddSongWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaPlayer.Visual"
        mc:Ignorable="d"
        Title="Add Song" Height="280" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Song Title -->
        <Label Content="Song Title:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox Name="SongTitleTextBox" Grid.Row="1" Width="300" Margin="0,5,0,10"/>

        <!-- Genre Selection -->
        <Label Content="Genre:" Grid.Row="2" VerticalAlignment="Center"/>
        <ComboBox Name="GenreComboBox" Grid.Row="3" Width="300" Margin="0,5,0,10"/>

        <!-- File Selection -->
        <Label Content="Select Media File:" Grid.Row="4" VerticalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" Grid.Row="5" Margin="0,5,0,10">
            <TextBox Name="FilePathTextBox" Width="250" IsReadOnly="True" Margin="0,0,10,0"/>
            <Button Content="Browse..." Click="SelectFile_Click" Width="100"/>
        </StackPanel>

        <!-- Confirm and Cancel Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Add Song" Click="AddSong_Click" Width="150" Margin="5"/>
            <Button Content="Cancel" Click="Cancel_Click" Width="150" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
